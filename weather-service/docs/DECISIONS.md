# Decisions

- Slim 4: minimal, PSR-compliant framework with low overhead for a small gateway.
- Redis: fast, simple cache backing and readiness dependency.
- HTTP sync (for now): aligns with current internal consumers; async/eventing to be evaluated later.
